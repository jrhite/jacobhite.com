---
/**
 * A self-hosted recording. Self-hosted rather than embedded from YouTube so the
 * page carries no third-party player, no tracking, and no dependency on an
 * account that could take the file down.
 *
 * `preload="metadata"` on purpose: the poster is a real still from the
 * recording, so the browser fetches a few kilobytes of headers instead of
 * megabytes of video nobody asked to play.
 */
interface Props {
  src: string;
  poster?: string;
  /** Accessible name for the player, and the visible caption above it. */
  title: string;
  /** Human-readable running time, e.g. "4:19". */
  duration?: string;
  /** One line under the player, for context the recording itself doesn't give. */
  caption?: string;
  /**
   * The recording's own aspect ratio, as `w / h`. Given rather than assumed
   * because the box is sized before the file loads — get it wrong and the
   * player letterboxes its own poster.
   */
  aspect: string;
}

const { src, poster, title, duration, caption, aspect } = Astro.props;
---

<figure class="video">
  <div class="video__head">
    <span class="video__label">Recording</span>
    {duration && <span class="video__duration">{duration}</span>}
  </div>

  <video
    class="video__player"
    style={`aspect-ratio: ${aspect};`}
    controls
    preload="metadata"
    poster={poster}
    title={title}
  >
    <source src={src} type="video/mp4" />
    <p>
      Your browser can’t play this video.
      <a href={src} download>Download the recording</a> instead.
    </p>
  </video>

  {caption && <figcaption class="video__caption">{caption}</figcaption>}
</figure>

<style>
  .video {
    /* The top margin is stated rather than left to `.prose > * + *`: a bare
       `margin: 0` here outranked that rule and sat the player flush against
       the heading above it. A player is a heavier block than a paragraph, so
       it takes more than the paragraph step. */
    margin: var(--s-5) 0 var(--s-7);
  }

  .video__head {
    display: flex;
    align-items: baseline;
    justify-content: space-between;
    gap: var(--s-3);
    margin-bottom: var(--s-2);
    font-family: var(--f-mono);
    font-size: var(--t-xs);
    letter-spacing: 0.06em;
    text-transform: uppercase;
  }

  .video__label {
    font-weight: 700;
    color: var(--accent-2);
  }

  .video__duration {
    color: var(--text-faint);
  }

  /* The recording is a slide deck: it is legible at the page's full width and
     cramped at the reading measure, so it runs wider than the prose below it.
     `aspect-ratio` is set inline, per recording. */
  .video__player {
    display: block;
    width: 100%;
    background: var(--ink-950);
    border: 1px solid var(--border);
    border-radius: var(--r-lg);
    box-shadow: var(--shadow-md);
  }

  .video__caption {
    margin-top: var(--s-3);
    font-size: var(--t-sm);
    color: var(--text-muted);
  }
</style>
